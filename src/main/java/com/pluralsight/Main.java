package com.pluralsight;
import java.io.IOException;

public class Main {
    /**
     * Main program loop, provides terminal interface for options and interacting with data file.
     */
    public static void main(String[] args) throws IOException {
        Display.start();
    }
    // look into abstract interfaces and lambdas for higher order functions - might use reflection API - not good performance
}
